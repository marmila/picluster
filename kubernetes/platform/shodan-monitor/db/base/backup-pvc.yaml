apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: apexforge-backup-pvc
  namespace: shodan-monitor
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 20Gi
