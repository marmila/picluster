apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: openai-api
  namespace: shodan-monitor
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: openai-secret
    creationPolicy: Owner
  data:
    - secretKey: OPENAI_API_KEY
      remoteRef:
        key: secret/data/ai/openai_api_key
        property: openai_api_key

---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: shodan-api
  namespace: shodan-monitor
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: shodan-secret
    creationPolicy: Owner
  data:
    - secretKey: SHODAN_API_KEY
      remoteRef:
        key: secret/data/shodan
        property: api_key

